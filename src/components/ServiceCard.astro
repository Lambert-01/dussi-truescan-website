---
const { title, description, href = "/services" } = Astro.props;
---
<article class="profile-card service-card">
  <div class="card-mark" aria-hidden="true">{title.slice(0, 2).toUpperCase()}</div>
  <h3><a href={href}>{title}</a></h3>
  <p>{description}</p>
</article>
